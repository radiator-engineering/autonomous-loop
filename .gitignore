# Regenerated by install.sh on every run; timestamped copies of the previous install.
.install-backup/

# macOS Finder metadata.
.DS_Store

# Rebuilt by install.sh --pack; not tracked.
autonomous-loop-share.zip
